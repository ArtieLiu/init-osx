- name: "Install brew casks"
  community.general.homebrew_cask:
    name: "{{ item }}"
  ignore_errors: yes
  with_items:
    - zoom
    - dozer
    - keepingyouawake
    - mirrordisplays
    - rancher
    - topnotch
    - monitorcontrol
    - raycast
    - visual-studio-code
    - intellij-idea
